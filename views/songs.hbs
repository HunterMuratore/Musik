<section>
    <ul>
        {{#each tracks as |track|}}
        <li>
            <a href="/song?id={{track.id}}&comment={{comment}}">{{track.album.name}}</a>
            <hr>
        </li>
        {{/each}}
    </ul>
</section>